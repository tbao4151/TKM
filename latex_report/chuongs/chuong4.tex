\section{TRIỂN KHAI, KIỂM THỬ VÀ ĐÁNH GIÁ}

\subsection{Môi trường triển khai}

Project được kiểm thử trên Ubuntu 24.04 với Mininet, Open vSwitch 3.3.4, iperf3 3.16, traceroute, tcpdump và Python 3.12. Linux kernel đã load các module MPLS cần thiết: \texttt{mpls\_router}, \texttt{mpls\_iptunnel}, \texttt{mpls\_gso}. Log môi trường được lưu tại \texttt{results/environment\_check.txt}.

\subsection{Pipeline tự động}

Toàn bộ quy trình có thể chạy bằng một lệnh:

\begin{lstlisting}[language=bash, caption={Lệnh chạy toàn bộ pipeline}]
cd Metro_Ethernet_MPLS_Project
sudo bash code/run_all.sh
\end{lstlisting}

Script thực hiện cleanup Mininet, kiểm tra môi trường, vẽ topology, dựng mạng MPLS, chạy ping/traceroute/iperf3/tcpdump, sinh \texttt{results.csv}, vẽ biểu đồ và tạo báo cáo Word.

\subsection{Giao diện giám sát và demo}

Bên cạnh các lệnh terminal, project có giao diện Tkinter để chọn cặp host nguồn--đích và chạy trực tiếp các phép đo ping, traceroute, throughput, delay, packet loss và jitter. Cửa sổ log được tách riêng để có thể phóng to/thu nhỏ khi cần quan sát toàn bộ output lệnh trong lúc thuyết trình.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\linewidth]{image/giaodiengui.png}
    \caption{Giao diện GUI monitor dùng trong phần demo}
\end{figure}

\subsection{Cấu hình MPLS kernel native}

MPLS và VPLS/L2VPN được cấu hình trong \texttt{code/network\_config.py}. Backbone PE/P chạy FRRouting OSPF/LDP để sinh route và label động. Các CE dùng WAN chung \texttt{172.16.100.0/24}; PE access interface không giữ IP khách hàng mà được đưa vào bridge VPLS/L2VPN. Data-plane L2VPN trong Mininet dùng full-mesh Linux GRETAP pseudowire có split-horizon/port-isolation trên underlay MPLS.

\begin{lstlisting}[language=bash, caption={Ví dụ route MPLS đã sinh trong kết quả}]
2.2.2.2/32 encap mpls 22 via 172.20.13.2 dev pe1-eth2 proto ospf
18 as to 19 via inet 172.20.13.1 dev p3-eth0 proto ldp
gt-pe2: gretap remote 2.2.2.2 key 100 master br-pe1
\end{lstlisting}

\subsection{Xác minh chuyển nhãn}

Tcpdump trên link core \texttt{p3-eth0} bắt được gói MPLS thật:

\begin{lstlisting}[language=bash, caption={Trích tcpdump MPLS}]
ethertype MPLS unicast (0x8847), length 102:
MPLS (label ..., tc 0, [S], ttl ...)
10.1.0.11 > 10.2.0.21: ICMP echo request
\end{lstlisting}

Kết quả trên chứng minh gói tin không chỉ đi bằng static IP route thường. Gói đi qua miền provider đã có MPLS shim header, label, TTL và payload ICMP bên trong.

\subsection{Bộ test hiệu năng}

Bộ test gồm hai nhóm:
\begin{itemize}
    \item Baseline: ping, traceroute, iperf3 TCP và iperf3 UDP 10M cho các cặp liên chi nhánh và nội bộ chi nhánh.
    \item UDP load sweep: iperf3 UDP ở 5M, 20M, 50M, 80M, 120M cho ba cặp liên chi nhánh để đo packet loss khi tải tăng.
\end{itemize}

\subsection{Bảng kết quả baseline}

\begin{table}[H]
\centering
\caption{Kết quả baseline từ results/results.csv}
\begin{tabularx}{\linewidth}{|X|X|r|r|r|r|}
\hline
\textbf{Nguồn} & \textbf{Đích} & \textbf{Throughput} & \textbf{Delay} & \textbf{Loss} & \textbf{Jitter} \\
 & & \textbf{Mbps} & \textbf{ms} & \textbf{\%} & \textbf{ms} \\
\hline
Branch1/host1 & Branch2/user1 & 55.074 & 33.240 & 0.000 & 0.196 \\
\hline
Branch1/host2 & Branch3/svr1 & 44.692 & 39.768 & 0.000 & 0.157 \\
\hline
Branch2/user1 & Branch3/svr2 & 54.623 & 35.537 & 0.000 & 0.410 \\
\hline
Branch1/host1 & Branch1/host4 & 94.713 & 6.633 & 0.000 & 0.098 \\
\hline
Branch2/admin1 & Branch2/guest1 & 94.057 & 12.954 & 0.000 & 0.118 \\
\hline
Branch3/svr1 & Branch3/os1 & 94.397 & 10.932 & 0.000 & 0.286 \\
\hline
\end{tabularx}
\end{table}

\subsection{Kết quả suy hao gói tin (packet loss) khi tải tăng}

Khi tải UDP tăng lên 80 Mbps và 120 Mbps, hiện tượng suy hao gói tin (packet loss) bắt đầu xuất hiện rõ rệt do tải cung cấp (offered load) vượt quá hoặc áp sát giới hạn năng lực của các liên kết backbone (50--60 Mbps). Điều này phản ánh chính xác tình trạng tắc nghẽn khi tải mạng tăng cao.

\begin{table}[H]
\centering
\caption{UDP packet loss sweep}
\begin{tabularx}{\linewidth}{|X|r|r|r|}
\hline
\textbf{Cặp đo} & \textbf{Load Mbps} & \textbf{Jitter ms} & \textbf{UDP loss \%} \\
\hline
Branch1 host1 -- Branch2 user1 & 5 & 0.137 & 0.000 \\
Branch1 host1 -- Branch2 user1 & 20 & 0.468 & 0.000 \\
Branch1 host1 -- Branch2 user1 & 50 & 0.359 & 0.000 \\
Branch1 host1 -- Branch2 user1 & 80 & 0.292 & 22.432 \\
Branch1 host1 -- Branch2 user1 & 120 & 0.243 & 47.167 \\
\hline
Branch1 host2 -- Branch3 svr1 & 5 & 1.104 & 0.000 \\
Branch1 host2 -- Branch3 svr1 & 20 & 0.554 & 0.000 \\
Branch1 host2 -- Branch3 svr1 & 50 & 0.352 & 0.000 \\
Branch1 host2 -- Branch3 svr1 & 80 & 0.347 & 32.556 \\
Branch1 host2 -- Branch3 svr1 & 120 & 0.331 & 54.156 \\
\hline
Branch2 user1 -- Branch3 svr2 & 5 & 0.126 & 0.000 \\
Branch2 user1 -- Branch3 svr2 & 20 & 0.101 & 0.000 \\
Branch2 user1 -- Branch3 svr2 & 50 & 0.233 & 0.000 \\
Branch2 user1 -- Branch3 svr2 & 80 & 0.298 & 22.507 \\
Branch2 user1 -- Branch3 svr2 & 120 & 0.291 & 47.165 \\
\hline
\end{tabularx}
\end{table}

\subsection{Biểu đồ so sánh hiệu năng}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{image/throughput_chart.png}
    \caption{Throughput đo bằng iperf3 TCP}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{image/delay_chart.png}
    \caption{Delay trung bình đo bằng ping}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{image/packet_loss_chart.png}
    \caption{UDP packet loss khi tải mạng tăng}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{image/jitter_chart.png}
    \caption{Jitter đo bằng iperf3 UDP}
\end{figure}

\subsection{Đánh giá ảnh hưởng kiến trúc LAN}

Kết quả cho thấy traffic nội bộ cùng chi nhánh có throughput gần 94--95 Mbps và delay thấp hơn traffic liên chi nhánh. Branch 1 có đường nội bộ ngắn nên delay nội bộ thấp nhất. Branch 2 có thêm các lớp access/distribution/core nên delay nội bộ cao hơn Branch 1. Branch 3 dùng leaf-spine nhưng do lab loop-free và cùng subnet, delay nội bộ vẫn thấp; khi đi liên chi nhánh, yếu tố chi phối chính là backbone provider và đường MPLS.

\subsection{Đánh giá so với định tuyến IP truyền thống}

Cấu hình định tuyến IP truyền thống được sử dụng làm cơ sở (baseline) để so sánh. Điểm khác biệt quan trọng của môi trường MPLS là các bộ định tuyến P không cần lưu trữ thông tin định tuyến tĩnh đến các mạng LAN khách hàng; thay vào đó, quá trình chuyển tiếp diễn ra hoàn toàn thông qua nhãn. Điều này minh chứng rõ nét vai trò của mạng lõi nhà cung cấp: lõi chỉ tập trung chuyển tiếp bằng nhãn, trong khi các bộ định tuyến biên (PE) chịu trách nhiệm ánh xạ dải địa chỉ của khách hàng vào các đường dẫn chuyển mạch nhãn (LSP).

\subsection{Nhận xét tổng hợp}

Mô hình đã mô phỏng thành công mạng Metro Ethernet dựa trên MPLS và cung cấp đầy đủ minh chứng về việc chuyển tiếp bằng nhãn thực tế. Hệ thống tích hợp FRRouting để vận hành OSPF và LDP trong các không gian mạng (namespace) của PE/P. Kết quả log ghi nhận OSPF đạt trạng thái \texttt{Full/-}, LDP hoạt động bình thường (\texttt{OPERATIONAL}), đồng thời thực hiện thành công việc phân bổ nhãn và thiết lập bảng chuyển tiếp MPLS. Việc triển khai thành công VPLS thông qua các kết nối GRETAP pseudowire càng làm rõ tính hiệu quả của kiến trúc phân lớp này.
